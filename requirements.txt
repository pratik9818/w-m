fastapi>=0.115
uvicorn[standard]>=0.32
aiogram>=3.15
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14
pydantic-settings>=2.6
redis>=5.2
python-slugify>=8.0
httpx>=0.27
# bot_api/services/queue.py imports arq to enqueue pipeline jobs -- without this the
# very first deploy dies with ModuleNotFoundError on import, since this file (not
# pyproject.toml) is what the serverless runtime installs from.
arq>=0.26
